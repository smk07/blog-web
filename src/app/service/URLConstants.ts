export const host = "http://localhost:3000";

export const loginURL = `${host}/signin`;
export const signupURL = `${host}/signup`;
export const verifiedURL = `${host}/verified`;
export const post = `${host}/posts/`